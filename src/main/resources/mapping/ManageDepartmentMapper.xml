<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
	   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.project.dao.ManageDepartmentDao">
	<resultMap id="departmentMap" type="com.web.project.model.BackManage.Department">
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="name" property="name" jdbcType="VARCHAR" />
		<result column="level" property="level" jdbcType="INTEGER" />
		<result column="cluster" property="cluster" jdbcType="INTEGER" />
		<result column="parent" property="parent" jdbcType="INTEGER" />
	</resultMap>

	<select id="getDepartment" resultMap="departmentMap">
		select * from department
	</select>
	
		<insert id="insertDepartmentInfo" >
	    insert into department(name,level,cluster,parent)
	    values (#{0},#{1},#{2},#{3})
    </insert>
    	<delete id="deleteDepartmentInfoById"  parameterType="java.util.List">
		delete from department
		<where>
			id in <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
				#{item}
			</foreach>
		</where>
	</delete>
</mapper>	 