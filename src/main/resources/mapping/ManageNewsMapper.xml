<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
	   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.project.dao.ManageNewsDao">	 
	<resultMap id="newsMap" type="com.web.project.model.BackManage.News" >
		<id column="id" property="id" jdbcType="INTEGER" /> 
		<result column="title" property="title" jdbcType="VARCHAR" />
		<result column="time" property="time" jdbcType="INTEGER" />
		<result column="type" property="type" jdbcType="VARCHAR" />
		<result column="content" property="content" jdbcType="VARCHAR" />
		<result column="fujian" property="fujian" jdbcType="VARCHAR" />
		<result column="fujiantype" property="fujianType" jdbcType="VARCHAR" />
		<result column="fujianname" property="fujianName" jdbcType="VARCHAR" />		
	</resultMap>    
	  
	<select id="getNewsByType" resultMap="newsMap"> 
		select * from makernews
		<where>
		type=#{type}
		</where>
		order  by time DESC;
	</select>
	
	<select id="getNewsDetailById" resultMap="newsMap"> 
		select * from makernews
		<where>
		id=#{id}
		</where>
	</select>
	
	<select id="getNewsInfo" resultMap="newsMap"> 
		select * from makernews

	</select>
	
		<insert id="insertNewsInfo" >
	    insert into makernews(title ,type, time, fujianType, fujianName,content)
	    
	    values (#{0},#{1},#{2},#{3},#{4},#{5})

    </insert>
    	<delete id="deleteNewsInfoById"  parameterType="java.util.List">
		delete from makernews
		<where>
			id in <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
				#{item}
			</foreach>
		</where>
	</delete>
	
</mapper>